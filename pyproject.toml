[tool.black]
line-length = 80
target-version = ["py311"]

[tool.isort]
profile = "google"
line_length = 80

[tool.mypy]
python_version = "3.11"
strict = true

[[tool.mypy.overrides]]
module = ["capstone", "yara"]
ignore_missing_imports = true
